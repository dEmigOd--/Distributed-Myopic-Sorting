\documentclass[a4paper, english]{article}

\usepackage{silence}
%Disable all warnings issued by latex starting with "You have..."
\WarningFilter{latex}{You have requested package}
\usepackage{"C:/Users/dmitry.ra/Desktop/Studies/Articles/PhD/Latex\space Technical/userdefinitions.v002"}
\usepackage{"C:/Users/dmitry.ra/Desktop/Studies/Articles/PhD/Latex\space Technical/additionaldefinitions.v001"}
\usepackage{geometry}

\begin{document}
	\pgfmathsetmacro{\lengthlabelline}{0.8}
	\pgfmathsetmacro{\labelxoffset}{0.5}
	\pgfmathsetmacro{\labelyoffset}{0.1}
	\pgfmathsetmacro{\tiklabelxoffset}{0.5}
	\pgfmathsetmacro{\tiklabelyoffset}{0.3}
	\pgfmathsetmacro{\width}{1.5}
	
	\newgeometry{left=2cm}
	\begin{figure}
		\centering
		\subfloat[State Machine of agent at state 1\label{subfig1}]
		{
			\def\numdirections{2}
			\def\directionone{N}
			\def\directiontwo{W}
			
			\newcommand{\decisionszero}{0/$\emptyset$, 2/N, 2/W, 2/N}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$}

			\input{template.with.values}
		}
		\subfloat[State Machine of agent at state 2\label{subfig2}]
		{
			\def\numdirections{2}
			\def\directionone{N}
			\def\directiontwo{E}

			\newcommand{\decisionszero}{0/$\emptyset$, 1/N, 0/$\emptyset$, 2/N}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$}

			\input{template.with.values}
		}
		\qquad
		\subfloat[State Machine of agent at state 3\label{subfig3}]
		{
			\def\numdirections{2}
			\def\directionone{E}
			\def\directiontwo{S}

			\newcommand{\decisionszero}{0/$\emptyset$, 3/$\emptyset$, 2/$\emptyset$, 3/$\emptyset$}
			\newcommand{\decisionsone}{2/$\emptyset$, 3/$\emptyset$, 2/$\emptyset$, 3/$\emptyset$}
			\newcommand{\decisionstwo}{2/$\emptyset$, 3/$\emptyset$, 2/S, 2/S}
			\newcommand{\decisionsthree}{2/$\emptyset$, 2/E, 2/S, 2/E}

			\input{template.with.values}
		}
		\subfloat[State Machine of agent at state 4\label{subfig4}]
		{
			\def\numdirections{2}
			\def\directionone{S}
			\def\directiontwo{W}

			\newcommand{\decisionszero}{0/$\emptyset$, 2/$\emptyset$, 0/$\emptyset$, 2/$\emptyset$}
			\newcommand{\decisionsone}{/, 2/$\emptyset$, /, 2/$\emptyset$}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/S, 2/$\emptyset$, 2/S}

			\input{template.with.values}
		}
		\qquad
		\subfloat[State Machine of agent at state 9\label{subfig9}]
		{
			\def\numdirections{2}
			\def\directionone{N}
			\def\directiontwo{S}

			\newcommand{\decisionszero}{0/$\emptyset$, 1/N, 1/$\emptyset$, 1/N}
			\newcommand{\decisionsone}{1/$\emptyset$, 2/N, 2/$\emptyset$, 2/$\emptyset$}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/$\emptyset$, 2/S, 2/S}

			\input{template.with.values}
		}
		\qquad
		\subfloat[State Machine of agent at state 5\label{subfig5}]
		{
			\def\numdirections{3}
			\def\directionone{N}
			\def\directiontwo{E}
			\def\directionthree{W}

			\newcommand{\decisionszero}{0/$\emptyset$, 1/N, 0/$\emptyset$, 2/W, 1/N, 1/N, 2/W, 1/N}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$}

			\input{template.with.values}
		}
	\end{figure}
	\begin{figure}\ContinuedFloat
		\centering
		\subfloat[State Machine of agent at state 6\label{subfig6}]
		{
			\def\numdirections{3}
			\def\directionone{N}
			\def\directiontwo{E}
			\def\directionthree{S}

			\newcommand{\decisionszero}{0/$\emptyset$, 1/N, 0/$\emptyset$, 1/$\emptyset$, 1/N, 1/N, 1/$\emptyset$, 1/N}
			\newcommand{\decisionsone}{1/$\emptyset$, 2/N, 1/$\emptyset$, 2/$\emptyset$, 2/N, 2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/$\emptyset$, 2/$\emptyset$, 2/S, 2/$\emptyset$, 2/S, 2/S, 2/S}

			\input{template.with.values}
		}
		\qquad
		\subfloat[State Machine of agent at state 7\label{subfig7}]
		{
			\def\numdirections{3}
			\def\directionone{E}
			\def\directiontwo{S}
			\def\directionthree{W}

			\newcommand{\decisionszero}{0/$\emptyset$, 1/$\emptyset$, 2/$\emptyset$, 0/$\emptyset$, 1/$\emptyset$, 1/$\emptyset$, 2/$\emptyset$, 1/$\emptyset$}
			\newcommand{\decisionsone}{2/$\emptyset$, 2/E, 2/$\emptyset$, 1/$\emptyset$, 2/E, 2/E, 2/$\emptyset$, 2/E}
			\newcommand{\decisionstwo}{2/$\emptyset$, 1/$\emptyset$, 2/S, 2/$\emptyset$, 2/S, 1/$\emptyset$, 2/S, 2/S}

			\input{template.with.values}
		}
		\qquad
		\subfloat[State Machine of agent at state 8\label{subfig8}]
		{
			\def\numdirections{3}
			\def\directionone{N}
			\def\directiontwo{S}
			\def\directionthree{W}

			\newcommand{\decisionszero}{0/$\emptyset$, 1/N, 2/$\emptyset$, 0/$\emptyset$, 1/N, 1/N, 2/$\emptyset$, 1/N}
			\newcommand{\decisionsone}{1/$\emptyset$, /, 2/$\emptyset$, 1/$\emptyset$, 2/$\emptyset$, /, 2/$\emptyset$, 2/$\emptyset$}
			\newcommand{\decisionstwo}{2/$\emptyset$, 2/$\emptyset$, 2/S, 2/$\emptyset$, 2/S, 2/$\emptyset$, 2/S, 2/S}

			\input{template.with.values}
		}
		\caption{Agent state machines at different positions on the patch, a tuple designates next memory state and a picked movement direction, or \textit{do nothing} otherwise. 
			Empty hatched cells should be treated as erroneous states. Input direction is a position of empty cell at the beginning of time tick.}
	\end{figure}
	\restoregeometry
\end{document}
